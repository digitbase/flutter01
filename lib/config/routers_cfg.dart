import 'package:flutter01/pages/lib_pages.dart';

var staticRoutes = {
  "/": (context) => IndexPages(),
  "/home": (contxt) => HomePage(),
  "/category": (contxt) => CategoryPage(),
  "/member": (contxt) => MemberPage(),
  "/cart": (contxt) => CartPage(),
};
